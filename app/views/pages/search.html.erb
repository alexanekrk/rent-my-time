<h1> Welcome to the search page </h1>
<div class="d-flex">
  <div>
    <%= render 'pigeons/pigeons_list' %>
  </div>

  <div class="sticky-top" style="width: 100%; height: 600px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
