
  <%= simple_form_for([@pigeon, @booking]) do |f| %>
  <%= f.input :start_date, as: :date, html5: true %>
  <%= f.input :end_date, as: :date, html5: true %>
  <div class='btn-flat'><%= f.button :submit %></div>
  <% end %>
